{
  "pluginAlias": "IPortSMButtonsLAN",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "iPort Buttons LAN"
      },
      "ip": {
        "title": "Device IP Address",
        "type": "string",
        "placeholder": "192.168.2.12",
        "description": "IP address of your iPort device"
      },
      "port": {
        "title": "Port",
        "type": "integer",
        "default": 10001,
        "description": "TCP port for iPort device communication"
      },
      "timeout": {
        "title": "Connection Timeout (ms)",
        "type": "integer",
        "default": 10000,
        "description": "Socket timeout in milliseconds"
      },
      "reconnectDelay": {
        "title": "Initial Reconnect Delay (ms)",
        "type": "integer",
        "default": 10000,
        "description": "Initial delay before reconnecting (will increase exponentially)"
      },
      "maxReconnectDelay": {
        "title": "Maximum Reconnect Delay (ms)",
        "type": "integer",
        "default": 300000,
        "description": "Maximum delay between reconnection attempts (5 minutes)"
      },
      "maxReconnectAttempts": {
        "title": "Maximum Reconnect Attempts",
        "type": "integer",
        "default": 10,
        "description": "Maximum number of reconnection attempts before giving up"
      },
      "directControlPort": {
        "title": "Direct Control HTTP Port",
        "type": "integer",
        "default": 3000,
        "description": "Local HTTP server port for direct button control"
      },
      "buttonMappings": {
        "title": "Button Mappings",
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "buttonNumber": {
              "title": "Button Number",
              "type": "integer",
              "minimum": 1,
              "maximum": 10
            },
            "modeColor": {
              "title": "LED Mode Color",
              "type": "string",
              "enum": ["yellow", "red", "green", "blue", "purple", "white", "any"],
              "default": "any",
              "description": "Execute only when iPort LED is this color (or 'any' for all)."
            },
            "actionType": {
              "title": "Action Type",
              "type": "string",
              "enum": ["lifx"],
              "default": "lifx"
            },
            "targetId": {
              "title": "Target LIFX Bulb(s)",
              "type": "array",
              "items": {
                "type": "string",
                "description": "LIFX bulb ID (MAC without ':', e.g. d073d5000001)"
              },
              "description": "List of LIFX IDs (bulbs controlled together)."
            },
            "action": {
              "title": "Action",
              "type": "string",
              "enum": ["on", "off", "brightness"],
              "default": "on"
            },
            "value": {
              "title": "Brightness Value (0–100)",
              "type": "integer",
              "minimum": 0,
              "maximum": 100,
              "description": "Required if action is 'brightness'"
            }
          },
          "required": ["buttonNumber", "modeColor", "actionType", "targetId", "action"],
          "allOf": [
            {
              "if": { "properties": { "action": { "const": "brightness" } } },
              "then": { "required": ["value"] }
            }
          ]
        }
      }
    },
    "required": ["name", "ip", "port", "buttonMappings"]
  }
}
